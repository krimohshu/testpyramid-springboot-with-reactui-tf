# Import profile: use Postgres (override via -Dspring.datasource.url/username/password)
# Disable Flyway for import runs to avoid baseline/schema history conflicts
spring.flyway.enabled=false
# keep JPA ddl-auto as update to allow importer to write
spring.jpa.hibernate.ddl-auto=update
